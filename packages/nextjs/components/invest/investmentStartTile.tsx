// components/invest/InvestmentStartTile.tsx

/*export const InvestmentStartTile = () => {
  const router = useRouter();

  return (
    <div className="bg-base-100 p-6 rounded-2xl shadow w-full max-w-sm flex flex-col space-y-4">
      <h2 className="text-lg font-semibold text-primary">📈 Start Investment</h2>
      <select className="select select-bordered w-full" onChange={e => router.push(`/invest/overview?q=${e.target.value}`)}>
        <option disabled selected>Select term (in quarters)</option>
        <option value="1">1 Quarter</option>
        <option value="2">2 Quarters</option>
        <option value="3">3 Quarters</option>
        <option value="4">4 Quarters</option>
      </select>
    </div>
  );
};*/
